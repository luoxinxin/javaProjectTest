server:
  port: 8102
spring:
  application:
    name: eureka-client
eureka:
  client:
    service-url:
      defaultZone: http://replica1:8002/eureka/,http://replica2:8003/eureka/
    register-with-eureka: true
    fetch-registry: true
